const myObject = {
    cpp: "C++",
    js: "JavaScript",
    rb: "Ruby",
    java: "Java"
}

// for (const key in myObject) {
    // console.log(`${key} : ${myObject[key]}`);
// }

const programming = ['cpp', 'js', 'rb', 'java', 'ruby']

// for (const key in programming) {
    // console.log(programming[key]);
// }

const map = new Map();

// map.set('IN', 'India')
// map.set('EN', 'England')
// map.set('FR', 'France')
// map.set('USA', 'United State of America')
// 
// for (const key in map) {
    // console.log(key);  // Doesn't work as map is not iterable
// }

